<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Thống kê tháng' }); %>

<body>
	<%- include('../partials/navbar'); %>
	<div class="container-fluid">
		<%- include("../partials/flash") %>
		<div class="row">
			<main class="col-12 px-4">
				<div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
					<h1>Tháng <%=month%>, năm <%=year%></h1>
				</div>
				<%- include("month-search.partial.ejs") %>
				<% const numOfPage = Math.ceil(count / numOfRowPerPage) %>
				<% if (count === 0 || pageNo > numOfPage ) { %>
				<!-- no data -->
				<div class="row">
					<div class="col-6 offset-3 alert alert-dark" role="alert">
						<h1>Không có dữ liệu</h1>
						<p>
							Không tìm thấy dữ liệu nào khớp với tìm kiếm.
							<!-- <a class="alert-link" href="/">Đặt lại</a> bộ tìm kiếm ? -->
						</p>
					</div>
				</div>
				<% } else { %>
				<div class="table-responsive small">
					<table class="index table table-bordered table-hover table-striped">
						<%- include("month-table-headers.partial.ejs") %>
						<%- include("month-table-body.partial.ejs") %>
					</table>
				</div>
				<%- include("month-table-bottom-nav.partial.ejs") %>
				<% } %>
			</main>
		</div>
	</div>
</body>

</html>